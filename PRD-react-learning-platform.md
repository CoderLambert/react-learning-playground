# React 学习实验室产品需求文档

- 文档版本：v0.1
- 文档状态：草案
- 产品名称：React 学习实验室
- 目标版本：MVP
- 更新时间：2026-09-06

> 本文档基于当前 `react-init` 项目进行设计。当前项目是 React + Vite 初始化模板，还没有真实的课程、练习和用户数据能力。文档中的用户目标、指标和课程内容属于待验证假设。

## 1. Summary

React 学习实验室是一个面向 React 初学者的轻量学习网站。它通过短知识卡、可阅读的代码示例和即时反馈练习，帮助用户从“看懂概念”走到“完成第一个 React 组件”。

MVP 先聚焦单用户、单浏览器场景：用户无需注册即可选择学习模块、完成练习并查看学习进度。进度保存在浏览器本地，不依赖后端服务，以适配当前 React + Vite 项目的技术边界。

## 2. Contacts

| 角色 | 负责人 | 参与内容 |
|---|---|---|
| 产品负责人 | 待定 | 目标、范围、优先级和验收 |
| 课程内容负责人 | 待定 | React 知识点、示例和练习答案 |
| 前端实现者 | 待定 | 页面、交互、本地进度和质量验证 |
| 体验评审者 | 待定 | 可理解性、可用性和移动端体验 |

## 3. Background

### 3.1 当前项目状态

当前仓库是 Vite 提供的 React 初始化模板，已有：

- React 19 和 React DOM
- Vite 开发与生产构建
- Oxlint 检查
- 响应式和深色模式样式
- 一个计数器示例
- Vite、React 文档和社区外链

当前还没有：

- 课程目录
- 学习内容模型
- 练习题
- 用户进度
- 路由或页面状态管理
- 后端、账号和数据服务

### 3.2 当前阻塞问题

`src/App.jsx` 中存在以下运行时错误：

```jsx
const user = null;
console.log(user.name);
```

组件在返回页面之前访问了 `null` 的属性，会导致页面空白。该问题必须在产品功能开发前修复，否则学习页面无法挂载。

### 3.3 为什么现在做

React 初始化环境已经完成，下一步需要把模板改造成一个有明确学习目标的练习产品。一个受控的学习平台比继续堆叠模板展示内容更适合作为 React 学习项目：它能同时练习组件拆分、状态管理、事件处理、条件渲染、列表渲染和浏览器存储。

### 3.4 用户问题

React 初学者通常可以找到大量零散教程，但经常遇到以下问题：

- 不知道应该按什么顺序学习
- 看完概念后不会写小功能
- 练习失败时不知道错在哪里
- 学习进度依赖手工记录
- 课程内容过长，难以形成连续反馈

### 3.5 核心 Job-to-be-Done

> 当我刚开始学习 React 时，我希望按照清晰的顺序完成短小练习，并立即知道自己是否理解了当前概念，这样我可以逐步建立编写 React 组件的信心。

## 4. Objective

### 4.1 产品目标

在不引入后端的前提下，提供一条可以独立完成的 React 入门学习路径，让用户在一次学习过程中完成：

1. 选择一个学习模块
2. 阅读一个短知识点
3. 查看一个最小代码示例
4. 完成至少一道练习
5. 获得即时反馈
6. 在刷新页面后继续查看自己的进度

### 4.2 用户价值

- 用短模块降低开始学习的门槛
- 用练习把阅读转化为动手
- 用反馈帮助用户定位错误
- 用进度状态维持学习连续性
- 不要求注册，减少第一次使用的阻力

### 4.3 项目价值

本项目同时作为 React 学习和开发练习，覆盖以下技术目标：

- 函数组件和组件组合
- `useState` 与受控交互
- 条件渲染和列表渲染
- 事件处理
- 可复用的卡片、按钮和进度组件
- `localStorage` 数据持久化
- 响应式布局和基础可访问性

### 4.4 MVP 成功指标

以下指标是首轮目标，不是已经验证的现状数据：

| 指标 | 目标 | 说明 |
|---|---:|---|
| 首次学习激活率 | ≥ 60% | 首次访问后打开第一个模块并开始练习的用户比例 |
| 首题完成率 | ≥ 50% | 开始学习后完成至少一道练习的用户比例 |
| 练习即时反馈可用率 | 100% | 每道练习提交后都显示正确/错误及解释 |
| 进度恢复成功率 | ≥ 95% | 刷新页面后仍能看到上次完成状态的测试比例 |
| 首次学习完成时间 | 10–20 分钟 | 完成第一个模块和练习的合理时间范围 |
| 关键任务成功率 | ≥ 80% | 测试用户能独立完成“进入模块→完成练习→查看进度” |

MVP 暂不接入埋点服务。第一阶段通过本地测试和 3–5 名目标用户的可用性测试验证指标；若结果达标，再增加正式数据采集。

## 5. Market Segment(s)

### 5.1 主要用户

**React 初学者**

- 已了解基础 HTML、CSS 和 JavaScript
- 刚接触组件、状态和 JSX
- 能运行一个前端项目，但缺少系统练习
- 希望通过较短的任务获得明确反馈
- 更关心“我能不能写出来”，而不只是阅读概念

### 5.2 次要用户

**有其他前端框架经验、正在迁移到 React 的开发者**

- 已理解组件化思想
- 想快速熟悉 React 的状态和事件模型
- 需要一条短路径复习核心概念

MVP 不以儿童、企业培训或高级 React 开发者为主要目标用户。

### 5.3 用户约束

- 学习时间通常较短，单次 10–20 分钟
- 可能使用手机或小尺寸笔记本
- 可能不愿意注册账号
- 需要避免一次展示过多新概念
- 需要能在刷新后保留基础进度

### 5.4 替代方案

- React 官方文档
- 视频课程
- 长篇博客或电子书
- 在线代码练习平台
- 直接跟随项目教程开发

我们的初步差异点不是内容数量，而是“短内容 + 小练习 + 即时反馈 + 连续进度”。

## 6. Value Proposition(s)

### 6.1 价值主张

> 用 10–20 分钟完成一个 React 小概念和一个练习，让初学者知道自己学会了什么、下一步应该学什么。

### 6.2 用户任务与收益

| 用户任务 | 当前困难 | 产品收益 |
|---|---|---|
| 理解 JSX | 概念抽象，示例分散 | 看到最小示例和结果 |
| 学习组件 | 不知道如何拆分 | 通过模块化案例理解职责 |
| 学习状态 | 只会照抄代码 | 通过交互练习理解状态变化 |
| 判断是否学会 | 看完教程后没有反馈 | 提交练习后立即获得解释 |
| 继续学习 | 容易忘记上次进度 | 首页显示已完成模块 |

### 6.3 首批学习模块建议

MVP 建议包含 5 个模块：

1. JSX 与组件
2. Props 与组件通信
3. `useState` 与事件
4. 条件渲染与列表渲染
5. 表单和受控输入

每个模块包含：

- 学习目标
- 3–5 个短知识点
- 至少 1 个代码示例
- 1–3 道练习题
- 完成状态

### 6.4 尚未验证的价值假设

- 初学者更愿意完成短练习，而不是连续观看长教程
- 即时解释比单纯显示“正确/错误”更能帮助理解
- 不注册也能保留本地进度，足以支持早期学习连续性
- 5 个核心模块足以让用户感受到入门进展

## 7. Solution

### 7.1 用户流程

```text
打开首页
  → 查看学习路径和总体进度
  → 选择一个未完成模块
  → 阅读学习目标和知识卡
  → 查看代码示例
  → 完成练习并提交
  → 查看答案解释
  → 标记模块完成
  → 返回首页查看进度
```

### 7.2 页面和组件

#### 首页 / 学习路径

展示：

- 产品名称和简短说明
- 总体完成进度
- 5 个学习模块卡片
- 每个模块的状态：未开始、进行中、已完成
- “继续学习”按钮

#### 模块详情页或视图

展示：

- 模块标题
- 学习目标
- 知识卡列表
- 代码示例
- 当前练习
- 上一题/下一题操作
- 模块进度

#### 练习反馈

提交答案后：

- 显示是否正确
- 显示正确答案
- 显示简短解释
- 允许继续下一题
- 不因答错而丢失当前模块进度

#### 进度状态

至少保存：

```js
{
  completedModules: [],
  currentModuleId: null,
  completedExercises: []
}
```

数据保存到浏览器 `localStorage`。读取失败或数据格式异常时，应恢复为空进度，而不是阻塞页面渲染。

### 7.3 核心功能需求

#### F1. 学习模块列表

- 系统展示 5 个 MVP 学习模块
- 每个模块显示标题、简介、预计学习时间和完成状态
- 用户可以打开任意模块
- 已完成模块仍可重复学习

#### F2. 学习内容展示

- 内容以短卡片分段展示
- 每张卡片只解释一个主要概念
- 代码示例使用等宽字体
- 示例必须标注预期结果或关键解释
- 关键术语首次出现时需要提供简单说明

#### F3. 练习提交

- 每道练习至少有一个明确问题
- 用户可以选择或填写答案
- 提交后立即显示反馈
- 答错时显示正确答案和原因
- 反馈不能只显示颜色，必须包含文字

#### F4. 模块完成

- 完成模块要求用户完成该模块的必做练习
- 完成后显示完成状态
- 用户可以回到首页查看总体进度
- 完成状态刷新页面后仍然保留

#### F5. 响应式和可访问性

- 在桌面和移动尺寸下都能完成核心流程
- 所有主要操作支持键盘访问
- 按钮和链接具有清晰文本
- 练习反馈使用文字和状态语义，不只依赖颜色
- 装饰图片使用空 `alt`，信息图片提供有效替代文本

#### F6. 错误恢复

- 初次打开时没有本地进度也能正常展示
- `localStorage` 不可用时，页面仍能使用，但刷新后不保证保留进度
- 本地数据解析失败时清除损坏数据并恢复默认状态
- 页面初始化异常不能静默显示空白页面，应提供可理解的错误提示

### 7.4 MVP 范围

#### 包含

- 单页面 React 学习应用
- 5 个静态学习模块
- 基础代码示例
- 选择题或短答案练习
- 即时反馈
- 本地进度保存
- 响应式布局
- 深色模式兼容
- 基础可访问性

#### 不包含

- 用户注册和登录
- 云端同步
- 社交、评论和排行榜
- 在线代码执行沙箱
- 课程后台管理系统
- AI 自动批改代码
- 支付和订阅
- 多语言内容
- 复杂路由和服务端渲染

### 7.5 技术方案约束

当前技术基础：

- React 19
- Vite 8
- JavaScript + JSX
- 原生 CSS
- Oxlint

MVP 技术建议：

- 先使用组件状态和静态数据完成首版
- 使用 `localStorage` 保存进度
- 暂不引入状态管理库
- 暂不引入后端和数据库
- 如果页面数量增加，再评估 React Router
- 只有当内容规模明显增长时，才引入独立内容数据文件或 TypeScript

### 7.6 关键假设和验证方式

| 假设 | 风险 | 验证方式 |
|---|---|---|
| 初学者能理解短知识卡 | 中 | 让 3 名初学者完成首个模块并访谈 |
| 选择题足以验证基础理解 | 中 | 比较练习完成率和复述正确率 |
| 本地进度足够支持 MVP | 低 | 测试刷新、关闭页面和异常数据场景 |
| 5 个模块能形成完整入门路径 | 高 | 观察用户是否能独立完成一个小 React 功能 |
| 用户不注册也愿意持续学习 | 高 | 观察第二次访问和模块继续率 |

### 7.7 验收标准

#### 首次使用

- Given 用户第一次打开应用
- When 页面加载完成
- Then 用户可以看到学习路径、模块卡片和总体进度
- And 页面不会因为没有用户数据而报错或空白

#### 开始学习

- Given 用户在首页看到未完成模块
- When 用户点击模块卡片
- Then 系统显示模块目标、知识内容和第一个练习

#### 提交练习

- Given 用户选择一个答案
- When 用户点击提交
- Then 系统显示正确/错误状态、正确答案和解释
- And 用户可以继续下一题

#### 完成模块

- Given 用户完成模块的必做练习
- When 用户点击完成或进入下一模块
- Then 系统将模块标记为已完成
- And 首页总体进度同步更新

#### 刷新恢复

- Given 用户已完成至少一个模块
- When 用户刷新页面
- Then 已完成模块仍显示为已完成
- And 用户可以继续上次学习位置

#### 异常数据

- Given 浏览器中的进度数据被修改为无效 JSON
- When 用户打开应用
- Then 应用恢复为默认空进度
- And 页面仍然可以使用

## 8. Release

### 8.1 发布阶段

#### Phase 0：可运行基础

目标：先修复当前模板阻塞并建立产品骨架。

内容：

- 删除或修复 `user.name` 的空值访问
- 将默认 Vite 页面替换为产品首页
- 定义模块和练习的静态数据结构
- 建立基础组件和样式变量
- 确认开发、lint 和生产构建流程正常

#### Phase 1：MVP 学习闭环

目标：用户可以完成一个完整学习模块。

内容：

- 学习路径首页
- 5 个学习模块
- 知识卡和代码示例
- 练习提交和反馈
- 模块完成状态
- `localStorage` 进度保存
- 移动端核心流程

#### Phase 2：验证与改进

目标：验证用户是否真的能完成学习任务。

内容：

- 邀请 3–5 名 React 初学者试用
- 记录任务完成率和卡点
- 优化难以理解的内容
- 补充错误状态和空状态
- 决定是否需要更多练习类型

### 8.2 后续版本候选

以下内容不进入 MVP，但可以根据验证结果安排：

- 用户账号和跨设备同步
- 在线代码编辑和安全执行
- 更丰富的练习类型
- 学习提醒
- 学习成就
- 课程搜索
- 内容管理后台
- AI 辅助解释和代码反馈
- 学习数据分析

### 8.3 相对时间范围

在单人开发、没有后端的前提下，建议使用相对时间而不是固定日期：

- Phase 0：1 个短迭代
- Phase 1：2–3 个短迭代
- Phase 2：1 个验证迭代

实际时间取决于课程内容准备、交互复杂度和测试反馈，不应在需求阶段承诺具体日期。

## 附录：当前项目到 MVP 的主要差距

| 当前状态 | 目标状态 |
|---|---|
| Vite 默认欢迎页 | React 学习路径首页 |
| 一个计数器 | 课程模块和练习状态 |
| 外部文档链接 | 内置学习内容和代码示例 |
| 无用户数据 | 本地学习进度 |
| 无业务组件 | 可复用学习卡、练习卡、进度组件 |
| 运行时存在空值错误 | 有初始化错误恢复能力 |
| 只有 lint/build | 增加核心用户流程 smoke test |
