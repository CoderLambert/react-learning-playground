# accessibility-basics 审核建议

## 当前结论

- Gate A Correctness：PASS
- Gate B Demo Contract：PASS
- Gate C Mental Model：PASS
- 综合：课程把“原生语义优先、ARIA 补充语义、键盘与真实辅助技术验证”作为主线，事实准确且 Demo 可实际完成键盘、label、状态反馈实验。

## 事实与版本边界

这部分主要是 Web 平台 / WAI-ARIA / WCAG 语义，不属于 React 19.2 特有能力。React 只负责声明 DOM 与属性；`role`、accessible name、键盘默认行为、focus 与 live region 的含义由 HTML/ARIA/浏览器/辅助技术共同定义。

“ARIA 不增加行为”表述准确：给 `div` 添加 `role="button"` 不会自动获得原生 button 的键盘、focus、disabled 与表单语义。优先原生元素是更稳健的默认策略。

## Mental Model

学习者可以回答三件关键问题：为什么原生语义比后补 ARIA 更可靠；什么时候确实需要 ARIA；为什么 axe 等自动化检查不能证明完整可访问性。课程没有把 a11y 简化成“加几个 aria-* 属性”。

## Demo ↔ Note

当前 Demo 与 Note 对齐：

- `label htmlFor` 与 input id 建立真实名称关系；
- 原生 submit button 可用键盘触发；
- loading 时使用原生 `disabled`；
- success/loading 使用 status/polite live region，error 使用 alert/assertive 反馈；
- Demo 明确展示原生 button 与 `div role="button"` 的行为差异边界，而不是要求学习者相信静态描述。

Note 要求的 Tab / Shift+Tab / Enter 键盘路径都可执行，因此 Gate B 通过。

## 本轮已关闭的边界与反例

- 已补充 placeholder 不能替代稳定 label，并说明帮助文本和错误信息应作为额外描述，而不是承担字段身份。
- 已补充 `aria-live` / `assertive` 不是越多越好；重复或过度 assertive 的播报会打断屏幕阅读器用户。Note 同时区分了 Demo 的 DOM 语义展示与生产消息紧急程度决策。
- 已补充设计系统覆盖浏览器 `outline` 后必须提供等价或更清晰的 visible focus indicator。

## 清晰度与工程决策

生产决策规则已经明确：先选正确原生元素，再补名称/状态；只有平台语义不足时才加 ARIA；自动化扫描之后仍需键盘和关键流程的真实辅助技术验证。

## 尚存人工验证债务

1. 已新增 `docs/manual-accessibility-checklist.md`，覆盖 sidebar/navigation、search、Demo controls、forms、validation error、live region 和 modal focus 生命周期；状态继续为 `real screen reader validation: PENDING`。
2. 保留“自动化 + 键盘 + 屏幕阅读器”的验证链，不把 axe 通过写成最终验收。除非真的使用 VoiceOver、NVDA 或 Orca 完成验证，否则不改为 PASS。
