# React 学习实验室静态页面设计说明

- 设计版本：v0.1
- 设计阶段：高保真开发交付
- 交付类型：页面设计规格，不直接修改业务代码
- 目标产品：React 学习实验室 MVP
- 目标用户：刚开始学习 React 的前端初学者
- 关联 PRD：`PRD-react-learning-platform.md`

> 本文档是基于当前 PRD 和现有 React + Vite 模板提出的设计方案。当前没有真实访谈、埋点或可用性测试数据，因此所有页面结构和视觉决策都属于待验证设计假设，不应被描述为已验证结论。

## 1. 设计目标

### 1.1 核心目标

让首次访问的 React 初学者在 30 秒内理解三件事：

1. 这里按照什么顺序学习 React
2. 我现在应该从哪里开始
3. 完成一次学习后，我能看到什么反馈

### 1.2 核心体验原则

1. **先给路径，再给内容**：首页先展示学习路线和下一步，不把用户丢进长文章。
2. **一次只处理一个概念**：知识卡、示例和练习围绕同一个学习目标组织。
3. **反馈必须解释原因**：答错时不仅显示结果，还解释为什么。
4. **进度可见且可恢复**：用户始终知道完成了多少，刷新后不会失去进度。
5. **保留 React 学习感**：视觉上使用代码编辑器、终端和模块卡片等开发者熟悉的隐喻，但不牺牲可读性。

### 1.3 当前设计检查点状态

| 检查点 | 状态 | 说明 |
|---|---|---|
| 问题由至少 3 条用户数据验证 | 未完成 | 当前只有 PRD 假设，需访谈或行为数据验证 |
| 信息架构由 card sort/tree test 验证 | 未完成 | 当前是基于学习顺序的初版 IA |
| 原型覆盖 happy path | 设计覆盖 | 首页→模块→练习→反馈→完成 |
| 原型覆盖至少一个错误状态 | 设计覆盖 | 错误答案、空进度和进度数据损坏 |
| 工程可行性确认 | 待确认 | 方案遵循现有 React/Vite 和无后端约束 |

## 2. 目标用户和关键任务

### 2.1 主要 Persona

**小林，React 初学者**

- 已会基础 HTML、CSS 和 JavaScript
- 能启动 Vite 项目，但不熟悉 JSX、Props 和 State
- 每次只有 10–20 分钟学习时间
- 容易在长教程中失去方向
- 需要明确的练习反馈来判断自己是否理解

### 2.2 关键任务

> 在不注册账号的情况下，找到下一个 React 学习模块，完成一题练习，理解答案，并能在刷新页面后继续学习。

### 2.3 用户旅程假设

| 阶段 | 用户行为 | 用户疑问 | 设计机会 |
|---|---|---|---|
| 进入 | 打开学习平台 | “这里能帮我学什么？” | 首屏说明结果和预计时间 |
| 选择 | 浏览 5 个模块 | “我该先学哪个？” | 强调推荐起点和顺序 |
| 学习 | 阅读知识卡和代码 | “这段代码做了什么？” | 每卡只讲一个概念 |
| 练习 | 选择答案并提交 | “我答对了吗？” | 显示即时结果和解释 |
| 继续 | 完成后返回路径 | “我接下来做什么？” | 自动推荐下一模块 |
| 恢复 | 刷新或再次打开 | “我的进度还在吗？” | 显示本地保存状态 |

## 3. 信息架构

### 3.1 MVP 站点结构

```text
React 学习实验室
├── 学习路径（首页）
│   ├── 总体进度
│   ├── 继续学习入口
│   └── 模块列表
│       ├── JSX 与组件
│       ├── Props 与组件通信
│       ├── useState 与事件
│       ├── 条件渲染与列表
│       └── 表单与受控输入
├── 学习模块
│   ├── 模块概览
│   ├── 知识卡
│   ├── 代码示例
│   ├── 练习题
│   ├── 反馈状态
│   └── 完成状态
└── 全局状态
    ├── 主题偏好（跟随系统）
    └── 本地学习进度
```

### 3.2 导航决策

MVP 不引入复杂路由。实现上可以使用单页状态切换：

- `home`：学习路径首页
- `module`：当前学习模块
- `completed`：模块完成反馈

如果后续需要分享模块链接或浏览器前进后退，再引入 React Router。

### 3.3 模块排序

模块按学习依赖排序，不允许一开始显示过多选择：

1. JSX 与组件
2. Props 与组件通信
3. `useState` 与事件
4. 条件渲染与列表渲染
5. 表单和受控输入

未完成的后续模块仍然可见，但使用“即将学习”状态；MVP 不强制锁死，避免用户无法探索。

## 4. 页面一：学习路径首页

### 4.1 页面目的

让用户快速理解产品价值、看到自己的进度，并开始或继续一条明确的学习路径。

### 4.2 桌面端布局

```text
┌──────────────────────────────────────────────────────────────┐
│ [</>] React 学习实验室                 [进度 2/5] [主题 ○]    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  学会 React，从一个小组件开始                                │
│  用短知识卡和即时练习，完成你的第一条 React 学习路径。        │
│                                                              │
│  [继续学习：useState 与事件 →]        已完成 2/5  ████░░░░   │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│  学习路径                                                     │
│  按顺序掌握 React 最常用的基础概念                             │
│                                                              │
│  [✓] 01  JSX 与组件                         已完成  8 分钟   │
│      写出你的第一个 React 组件                  [复习]       │
│                                                              │
│  [✓] 02  Props 与组件通信                   已完成  10 分钟  │
│      让组件接收并展示外部数据                  [复习]       │
│                                                              │
│  [→] 03  useState 与事件                    正在学习  12 分钟│
│      让页面对用户操作做出响应                  [开始学习]   │
│                                                              │
│  [ ] 04  条件渲染与列表                     未开始  12 分钟  │
│      根据数据决定页面显示什么                 [查看模块]    │
│                                                              │
│  [ ] 05  表单与受控输入                     未开始  15 分钟  │
│      构建可预测的表单交互                     [查看模块]    │
│                                                              │
├──────────────────────────────────────────────────────────────┤
│  学习提示：每个模块约 10–15 分钟，完成后进度会自动保存。      │
└──────────────────────────────────────────────────────────────┘
```

### 4.3 首页内容规格

#### 顶部导航

- 左侧：代码符号 Logo + “React 学习实验室”
- 中间：桌面端显示当前进度摘要
- 右侧：主题状态入口，MVP 默认跟随系统，不提供复杂设置
- 移动端隐藏进度摘要，只保留 Logo 和主题入口
- Logo 为首页链接，使用明确的 `aria-label="返回学习路径首页"`

#### 首屏 Hero

标题：

> 学会 React，从一个小组件开始

副标题：

> 用短知识卡和即时练习，完成你的第一条 React 学习路径。

主按钮：

- 有进度：`继续学习：{模块名}`
- 无进度：`开始第一课`

辅助信息：

- `5 个模块`
- `约 60 分钟`
- `无需注册`

主按钮必须是首屏唯一的 Primary CTA，避免用户同时面对多个等权操作。

#### 进度摘要

- 使用线性进度条，不使用圆环作为唯一表达
- 同时显示数字：`已完成 2/5 个模块`
- 进度条必须带可读文本或 `aria-valuenow`
- 没有进度时显示：`从第 1 课开始，完成后会自动保存进度`

#### 模块卡片

每张卡片包含：

- 序号
- 状态图标和状态文字
- 模块标题
- 一句话说明
- 预计时间
- 操作按钮

卡片状态：

- `completed`：绿色勾选、按钮为次要操作“复习”
- `in-progress`：紫色强调边框、按钮为主要操作“继续学习”
- `not-started`：中性边框、按钮为次要操作“查看模块”
- `unavailable`：仅在未来扩展使用，不作为 MVP 默认状态

### 4.4 首页响应式规则

#### 桌面端（≥ 960px）

- 最大内容宽度：1120px
- 页面左右内边距：32px
- Hero 使用左右布局：文案占 60%，进度摘要占 40%
- 模块卡片使用单列，保证说明和操作清晰

#### 平板端（640–959px）

- 页面左右内边距：24px
- Hero 改为上下布局
- 导航隐藏文字型进度摘要
- 卡片保持单列

#### 移动端（< 640px）

- 页面左右内边距：16px
- 标题字号降至 32px
- 主按钮宽度 100%
- 模块卡片内部按钮宽度 100%
- 卡片操作区域固定在内容底部
- 不依赖 hover 展示重要信息

## 5. 页面二：学习模块页

### 5.1 页面目的

让用户在一个连续、低干扰的页面中完成知识阅读、代码理解和练习反馈。

### 5.2 桌面端布局

```text
┌──────────────────────────────────────────────────────────────┐
│ ← 学习路径       03 / 05 · useState 与事件       进度 ███░░   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  useState 与事件                                              │
│  让组件记住信息，并对用户操作做出响应。                       │
│                                                              │
│  本节目标                                                     │
│  • 理解 state 和普通变量的区别                                │
│  • 使用事件更新 state                                         │
│                                                              │
│  知识卡 1 / 3                                                 │
│  state 是组件需要记住的、会随交互变化的数据。                  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ const [count, setCount] = useState(0)                   │  │
│  │                                                        │  │
│  │ <button onClick={() => setCount(count + 1)}>           │  │
│  │   Count is {count}                                     │  │
│  │ </button>                                               │  │
│  └────────────────────────────────────────────────────────┘  │
│  结果：点击按钮后，页面会显示最新的 count。                    │
│                                                              │
│  练习 1 / 2                                                   │
│  点击按钮后，哪一行代码会触发页面更新？                        │
│                                                              │
│  ○ const count = 0                                            │
│  ○ setCount(count + 1)                                       │
│  ○ console.log(count)                                        │
│                                                              │
│  [提交答案]                                                   │
│                                                              │
│  [上一张知识卡]                              [下一张 →]       │
└──────────────────────────────────────────────────────────────┘
```

### 5.3 模块页内容结构

1. 返回学习路径
2. 模块进度和模块标题
3. 本节学习目标
4. 知识卡
5. 代码示例
6. 示例结果说明
7. 练习题
8. 即时反馈
9. 上一张 / 下一张 / 完成本节操作

### 5.4 阅读体验

- 正文最大行宽：680px
- 代码块最大行宽：760px
- 每段正文不超过 3–4 行再换段
- 代码示例默认展示 8–14 行以内
- 关键词使用强调色，不使用大段粗体
- 代码块提供“复制代码”按钮，但复制不是完成学习的必要条件

## 6. 练习反馈状态

### 6.1 默认状态

- 选项未选中时，“提交答案”按钮 disabled
- 选项使用整行可点击区域，不只让用户点击小圆点
- 选中项使用紫色边框和浅色背景
- 键盘 focus 与选中状态必须同时可见

### 6.2 答对状态

```text
┌─────────────────────────────────────────┐
│ ✓ 回答正确                               │
│ setCount 会更新 state，并触发组件重新渲染。│
│                                         │
│ [下一题 →]                              │
└─────────────────────────────────────────┘
```

- 背景使用低饱和绿色
- 提供文字“回答正确”，不能只使用勾号
- 下一步按钮成为主要操作

### 6.3 答错状态

```text
┌─────────────────────────────────────────┐
│ ! 还差一点                               │
│ 正确答案是：setCount(count + 1)          │
│                                         │
│ setCount 会通知 React 更新 state，        │
│ 普通的 console.log 不会更新页面。         │
│                                         │
│ [再试一次]              [查看下一题 →]   │
└─────────────────────────────────────────┘
```

- 背景使用低饱和红色或橙色
- 保留用户已选答案
- 错误状态不阻塞用户继续学习
- 解释必须说明“为什么”，而不只是给出结果

### 6.4 模块完成状态

```text
┌─────────────────────────────────────────┐
│          ✓ 模块完成                      │
│  你已经掌握 useState 与事件的基础用法。   │
│                                         │
│  学习进度：3 / 5                         │
│                                         │
│ [返回学习路径]          [下一模块 →]     │
└─────────────────────────────────────────┘
```

下一模块按钮仅在存在下一模块时显示；最后一个模块显示“完成学习路径”。

## 7. 错误和空状态

### 7.1 初次访问空进度

文案：

> 还没有学习进度，从第一课开始吧。

按钮：

> 开始第一课

不要显示空白进度条或 `0%` 作为唯一内容。

### 7.2 本地进度读取失败

文案：

> 暂时无法读取上次的进度，我们会从当前页面继续。你仍然可以正常学习。

操作：

- `从第一课开始`
- 在开发环境记录错误信息

### 7.3 页面初始化错误

文案：

> 页面加载遇到问题，请刷新后重试。

操作：

- `重新加载`
- 不显示技术堆栈给普通用户

### 7.4 内容缺失

文案：

> 这个模块的内容还在准备中，先学习下一模块吧。

不允许渲染空卡片、空标题或只有一个无意义的返回按钮。

## 8. 视觉设计系统

设计延续当前项目已有的紫色强调色和系统字体，但把模板变量整理成产品语义 token。

### 8.1 颜色 Token

```css
:root {
  --color-bg: #ffffff;
  --color-surface: #faf9fc;
  --color-surface-raised: #ffffff;
  --color-text: #24202b;
  --color-text-muted: #6b6375;
  --color-border: #e5e4e7;
  --color-border-strong: #cbc7d2;
  --color-primary: #8f2de2;
  --color-primary-hover: #7420be;
  --color-primary-soft: rgba(170, 59, 255, 0.10);
  --color-success: #137a4b;
  --color-success-soft: #e9f7ef;
  --color-warning: #996000;
  --color-warning-soft: #fff6df;
  --color-error: #b42318;
  --color-error-soft: #fff0ee;
  --color-code-bg: #f4f3ec;
}

@media (prefers-color-scheme: dark) {
  :root {
    --color-bg: #16171d;
    --color-surface: #1c1d25;
    --color-surface-raised: #24252f;
    --color-text: #f3f4f6;
    --color-text-muted: #b4b1bd;
    --color-border: #363743;
    --color-border-strong: #505160;
    --color-primary: #c084fc;
    --color-primary-hover: #d2a1ff;
    --color-primary-soft: rgba(192, 132, 252, 0.16);
    --color-success: #6ee7a0;
    --color-success-soft: #123a28;
    --color-warning: #f4c76a;
    --color-warning-soft: #3b2c0d;
    --color-error: #ff9b91;
    --color-error-soft: #4a201c;
    --color-code-bg: #1f2028;
  }
}
```

状态颜色必须同时满足：

- 有文字标签
- 有图标或形状差异
- 有足够对比度
- 不依赖颜色作为唯一信息来源

### 8.2 字体 Token

```css
:root {
  --font-sans: system-ui, 'Segoe UI', Roboto, sans-serif;
  --font-mono: ui-monospace, SFMono-Regular, Consolas, monospace;
  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.125rem;
  --text-xl: 1.5rem;
  --text-2xl: 2.25rem;
  --text-hero: clamp(2rem, 5vw, 3.5rem);
}
```

### 8.3 间距 Token

以 4px 为基础单位：

```css
:root {
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  --space-10: 40px;
  --space-12: 48px;
  --space-16: 64px;
}
```

### 8.4 圆角和阴影

```css
:root {
  --radius-sm: 6px;
  --radius-md: 10px;
  --radius-lg: 16px;
  --radius-pill: 999px;
  --shadow-card: 0 8px 24px rgba(31, 23, 45, 0.08);
  --shadow-focus: 0 0 0 3px rgba(170, 59, 255, 0.24);
}
```

卡片不使用过重阴影。默认依靠边框区分层级，hover 时只提升少量阴影。

## 9. 组件规格

### 9.1 Button

变体：

- `primary`：开始学习、提交答案、下一模块
- `secondary`：复习、查看模块、返回
- `tertiary`：复制代码、次要导航
- `danger`：MVP 不使用

尺寸：

- `sm`：32px 高，用于辅助操作
- `md`：40px 高，默认
- `lg`：48px 高，用于首屏 CTA

状态：

- default
- hover
- active
- focus-visible
- disabled
- loading（后续接入异步数据时使用）

按钮规范：

- 可点击区域不小于 40×40px
- 不用纯图标代替核心操作文字
- disabled 状态仍需要保留可读对比度

### 9.2 ModuleCard

属性：

```text
id
number
title
description
duration
status: completed | in-progress | not-started
onOpen
```

视觉结构：

```text
状态图标 + 编号
标题                         时长
描述
状态文案                     操作按钮
```

### 9.3 ProgressBar

属性：

```text
value
max
label
size: sm | md
```

要求：

- 使用原生 `role="progressbar"` 语义或等价实现
- 提供 `aria-valuenow`、`aria-valuemin`、`aria-valuemax`
- 同时显示文本百分比或完成数量

### 9.4 KnowledgeCard

包含：

- 卡片编号
- 概念标题
- 解释正文
- 代码示例（可选）
- 示例结果
- 下一步操作

### 9.5 AnswerOption

状态：

- default
- hover
- focus
- selected
- correct
- incorrect
- disabled-after-submit

提交后不要让所有选项都变成不可读的低对比度灰色；正确答案和用户选择必须清晰可区分。

## 10. 交互说明

### 10.1 首页到模块

- 点击整个模块卡片或明确按钮都可以打开模块
- 打开“正在学习”模块时，默认回到上次未完成的知识卡
- 新模块默认从第一张知识卡开始
- 页面切换后焦点移动到模块标题

### 10.2 练习提交

- 未选择答案时禁止提交
- 提交后立即展示反馈，不使用延迟动画伪造加载
- 答错后允许重新选择，但保留错误解释
- 进入下一题前，用户必须已经看到反馈

### 10.3 进度保存

保存时机：

- 完成知识卡
- 提交练习
- 完成模块
- 用户离开模块前的状态更新

建议 key：

```text
react-learning-progress:v1
```

保存失败时页面继续工作，并显示非阻塞提示。

## 11. 可访问性要求

- 页面必须有唯一的 `h1`
- 标题层级按 `h1 → h2 → h3` 使用
- 所有表单或选项组有可读标签
- 键盘可以完成“打开模块→选择答案→提交→下一题”
- `:focus-visible` 轮廓不能被移除
- 文本和背景满足 WCAG AA 对比要求
- 反馈状态使用文本、图标和颜色三种线索
- 不用 hover 展示唯一信息
- 页面切换后将焦点移动到新页面主标题
- 装饰性 SVG 设置 `aria-hidden="true"`
- 代码示例支持复制，但复制按钮需要有明确标签
- 移动端不使用横向滚动承载核心内容

## 12. 开发交付映射

建议组件拆分：

```text
src/
├── App.jsx
├── components/
│   ├── AppHeader.jsx
│   ├── ProgressSummary.jsx
│   ├── ModuleCard.jsx
│   ├── ModuleList.jsx
│   ├── KnowledgeCard.jsx
│   ├── CodeBlock.jsx
│   ├── ExerciseCard.jsx
│   ├── AnswerOption.jsx
│   ├── FeedbackPanel.jsx
│   └── EmptyState.jsx
├── data/
│   └── learningModules.js
├── hooks/
│   └── useLearningProgress.js
├── App.css
└── index.css
```

实现顺序：

1. 先修复 `user.name` 空值访问，确保应用可挂载
2. 建立静态模块数据
3. 实现首页和 `ModuleCard`
4. 实现模块页、知识卡和代码块
5. 实现练习选项与反馈状态
6. 接入 `localStorage`
7. 补充空状态、错误状态和移动端样式
8. 做键盘和屏幕尺寸验证

MVP 暂不需要：

- 后端接口层
- 账号系统
- 复杂状态管理库
- 在线代码执行沙箱
- 完整路由系统

## 13. 设计验收清单

### 视觉

- [ ] 首页首屏只有一个主要 CTA
- [ ] 模块状态可以不依赖颜色被识别
- [ ] 桌面端和移动端层级一致
- [ ] 代码块、卡片和反馈面板有明确边界
- [ ] 浅色和深色主题都保持可读

### 交互

- [ ] 新用户能在 30 秒内找到开始入口
- [ ] 用户可以完成首页→模块→练习→反馈的 happy path
- [ ] 答错后能看到原因并继续学习
- [ ] 模块完成后能回到路径并看到进度变化
- [ ] 刷新后能恢复进度
- [ ] 本地数据损坏时不会白屏

### 可访问性

- [ ] 键盘可以完成核心学习流程
- [ ] 所有主要控件有可见 focus
- [ ] 反馈状态有文本说明
- [ ] 进度条有语义属性和文本值
- [ ] 标题层级正确
- [ ] 移动端没有核心内容横向溢出

### 验证计划

在设计进入开发前，建议进行：

1. 访谈至少 3 名符合条件的 React 初学者，验证学习顺序和痛点。
2. 对“学习路径、模块、练习、进度”做一次 closed card sort 或 tree test。
3. 使用 5–8 名参与者测试三个任务：开始第一课、完成练习、刷新后继续学习。
4. 记录任务完成率、完成时间、错误次数和 SUS 分数。
5. 对关键问题标记严重度：阻塞、高、中、低。

当前设计可以先作为静态页面开发规格，但在没有完成上述验证前，不应扩大到账号、社区或在线代码执行等功能。
